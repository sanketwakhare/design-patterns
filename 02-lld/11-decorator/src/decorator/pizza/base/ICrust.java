package decorator.pizza.base;

import decorator.pizza.IBase;

// multiple bases over bases are allowed but once toppings is inserted, no new base can be added
public interface ICrust extends IBase {
}
